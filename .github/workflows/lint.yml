name: Lint

env:
  TOOLKIT_VERSION: "from-requirements-txt"

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/**"
      - ".github/actions/**"
      - "requirements.txt"
      - "scripts/**"
      - "tools/**"
      - "tests/**"
      - "pyproject.toml"

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Python setup
        uses: ./.github/actions/python-setup
        with:
          extra-packages: ruff mypy types-pyyaml types-requests pytest pytest-cov pytest-timeout
          extra-requirements: tools/clean-query-mcp/requirements.txt

      - name: Ruff
        run: ruff check scripts/ tools/

      - name: Mypy
        run: mypy scripts/ tools/

      - name: Pytest
        run: python -m pytest tests/ -q --tb=line --cov=scripts --cov=tools/clean-query-mcp --cov-report=term --cov-fail-under=55
