# Dipendenti pubblici 2021-2023

## Stato

Questo candidato e stato promosso fuori da `dataset-incubator`.

Target attuale:
- `dataciviclab/analisi/dipendenti-pubblici`

Issue pubblica collegata:
- `dataciviclab#131`

## Nota

Il contratto tecnico (`dataset.yml`, `sql/`) resta in `dataset-incubator` come riferimento permanente.
Il layer pubblico (README civico, notebook) vive in `dataciviclab/analisi/dipendenti-pubblici`.

In `dataset-incubator` resta questa traccia di handoff.
